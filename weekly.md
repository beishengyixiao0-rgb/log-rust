# 每周工作


## 第 6 周

### 一、工作内容

1. **基础模块实现**
   - 完成 `level.rs`：日志级别枚举定义
     - 实现 LogLevel 枚举（Debug/Info/Warn/Error/Fatal/Off）
     - 实现 as_str() 和 from_str() 双向转换
     - 实现 Default、Display trait
   - 完成 `message.rs`：日志消息结构体
     - 定义 LogMessage 结构体
     - 实现时间戳和线程 ID 自动填充 


### 二、遇到的问题与解决方案

| 问题                  | 解决方案                           |
| --------------------- | ---------------------------------- |
| 时间戳获取 API 不熟悉 | 使用 SystemTime 和 UNIX_EPOCH 计算 |

### 三、下周工作计划

- 实现formatter.rs格式化器模块


## 第 7 周

### 一、工作内容

1. **格式化器模块实现**
   - 完成 `formatter.rs` 核心实现
   - 定义 FormatItem trait 和 10 种格式项
   - 实现模式解析器，支持格式符：%d、%p、%c、%t、%f、%l、%m、%n、%T
   - 支持自定义时间格式：%d{%Y-%m-%d %H:%M:%S}

2. **单元测试**
   - 为 formatter.rs 编写单元测试


### 二、遇到的问题与解决方案

| 问题                      | 解决方案                                |
| ------------------------- | --------------------------------------- |
| 策略模式的 trait 对象设计 | 使用 Arc<dyn FormatItem> 实现运行时多态 |


### 三、下周工作计划

- 实现 sink.rs 输出目标模块
 

## 第 8 周

### 一、工作内容

1. **输出目标模块实现（部分）**
   - 完成 `sink.rs` 框架设计
   - 实现 StdoutSink（标准输出）
   - 实现 FileSink（文件输出）
   - 定义 LogSink trait，统一不同输出目标的接口

2. **单元测试** 
   - 为 sink.rs 编写单元测试

### 二、遇到的问题与解决方案

| 问题                         | 解决方案                                      |
| ---------------------------- | --------------------------------------------- |
| 不同输出目标的写入方式不一致 | 抽象 LogSink trait，由 stdout/file 等分别实现 |
| 文件输出需要自动创建目录     | 在 FileSink 初始化时判断父目录并创建          |

### 三、下周工作计划

- 完成 RollSink 滚动文件输出
- 实现 buffer.rs 缓冲区模块


## 第 9 周

### 一、工作内容

1. **滚动文件输出实现**
   - 完成 `RollSink` 滚动文件输出目标
   - 支持按文件大小自动切分日志文件
   - 使用时间戳和序号生成滚动日志文件名，避免文件名冲突

2. **缓冲区模块实现**
   - 完成 `buffer.rs`
   - 实现 Buffer 的写入、读取、消费、重置和交换功能
   - 实现 SharedBuffer，使用 Mutex 封装线程安全访问

3. **示例补充**
   - 更新 `file_sink.rs` 示例
   - 演示普通文件输出和滚动文件输出

### 二、遇到的问题与解决方案

| 问题                       | 解决方案                                      |
| -------------------------- | --------------------------------------------- |
| 滚动日志文件名可能重复     | 在文件名中加入毫秒时间戳和递增序号            |
| 异步日志需要批量交换缓冲区 | 使用 push/pop 两个缓冲区，通过 swap 降低拷贝 |

### 三、下周工作计划

- 实现异步日志后台线程 `looper.rs`
- 完成 logger 核心结构和构建器


## 第 10 周

### 一、工作内容

1. **异步日志后台线程实现**
   - 完成 `looper.rs`
   - 使用后台线程处理异步日志写入
   - 使用 Condvar 和 Mutex 等同步工具控制数据等待和唤醒
   - 支持日志器释放时停止后台线程并回收资源

2. **日志器核心实现**
   - 完成 `logger.rs` 中的 SyncLogger 和 AsyncLogger
   - 实现 LoggerBuilder，支持名称、级别、同步/异步类型、格式化器和 sink 配置
   - 实现根日志器 root_logger
   - 实现命名日志器注册和查询

3. **日志宏实现**
   - 实现 `debug!`、`info!`、`warn!`、`error!`、`fatal!`
   - 实现指定日志器宏 `LOG_DEBUG!`、`LOG_INFO!`、`LOG_WARN!`、`LOG_ERROR!`、`LOG_FATAL!`

### 二、遇到的问题与解决方案

| 问题                         | 解决方案                                             |
| ---------------------------- | ---------------------------------------------------- |
| 同步和异步日志器需要统一接口 | 定义 LoggerTrait，并用 Arc<dyn LoggerTrait> 包装实现 |
| 全局根日志器需要安全初始化   | 使用 once_cell::sync::Lazy 实现单例管理              |

### 三、下周工作计划

- 增加全局统计模块
- 增加 panic hook
- 完善 README 和基础示例


## 第 11 周

### 一、工作内容

1. **日志统计模块实现**
   - 完成 `stats.rs`
   - 统计总日志数和 DEBUG/INFO/WARN/ERROR/FATAL 各级别数量
   - 实现 QPS 和错误率计算
   - 实现统计报告输出函数

2. **panic hook 实现**
   - 完成 `panic_hook.rs`
   - 捕获 panic 发生位置和 payload
   - panic 时输出当前日志统计信息，方便定位问题

3. **终端 TUI 初步实现**
   - 完成 `tui.rs` 基础界面
   - 使用 ratatui 和 crossterm 显示简单运行指标

4. **示例与文档更新**
   - 更新 `basic.rs` 和 `async_demo.rs`
   - 补充同步日志、异步日志和日志级别说明

### 二、遇到的问题与解决方案

| 问题                         | 解决方案                                      |
| ---------------------------- | --------------------------------------------- |
| 多线程统计需要避免锁竞争     | 使用 AtomicU64 记录各类计数                   |
| panic 时还要保留原始错误信息 | 在 hook 中输出 location、payload 和统计报告   |

### 三、下周工作计划

- 增加 gzip 压缩归档
- 增强滚动策略和旧日志清理
- 开始设计 Web Dashboard


## 第 12 周

### 一、工作内容

1. **日志压缩归档实现**
   - 完成 `compressor.rs`
   - 使用 flate2 将旧滚动日志压缩为 `.gz`
   - 压缩完成后删除原始 `.log` 文件
   - 使用独立线程执行压缩，避免阻塞日志写入

2. **滚动策略增强**
   - 支持按小时滚动
   - 支持按日期滚动
   - 支持大小和时间组合滚动策略
   - 增加 CleanupPolicy，支持按最大文件数和最大保留时间清理旧日志

3. **结构化日志格式**
   - 增加 JsonFormatter
   - 支持输出 JSON Lines 格式日志
   - 保留 logger 名称、级别、消息、文件、行号、线程 ID 和时间戳字段

### 二、遇到的问题与解决方案

| 问题                         | 解决方案                                          |
| ---------------------------- | ------------------------------------------------- |
| 压缩旧日志可能影响写入性能   | 将压缩任务放入后台线程处理                        |
| 多种滚动条件组合后逻辑复杂   | 抽象 RollingPolicy，分别处理 Size、Time、组合策略 |
| JSON 输出需要保证字段稳定    | 使用 serde_json 构造结构化对象                    |

### 三、下周工作计划

- 实现 MySQL 日志写入
- 实现最近日志缓存
- 为 Dashboard 查询做数据准备


## 第 13 周

### 一、工作内容

1. **MySQL Sink 实现**
   - 增加 `MysqlSink`
   - 使用 mysql crate 建立连接池
   - 将日志结构化写入 MySQL 表
   - 保存 logger_name、level、message、file、line、thread_id、created_at 等字段

2. **异步结构化 sink 处理**
   - 为 LogSink 增加 `log_message` 和 `accepts_batch`
   - 普通文本 sink 支持异步批量写入
   - MySQL 这类结构化 sink 直接接收完整 LogMessage，避免丢失元数据

3. **最近日志缓存实现**
   - 完成 `recent.rs`
   - 使用 VecDeque 保存最近日志
   - 支持按数量、级别和关键字查询
   - 为 Dashboard 实时日志列表提供数据来源

4. **MySQL 示例**
   - 增加 `mysql_demo.rs`
   - 单独验证 MySQL 写入流程

### 二、遇到的问题与解决方案

| 问题                             | 解决方案                                                  |
| -------------------------------- | --------------------------------------------------------- |
| 异步日志批量写入会丢失结构化字段 | 为结构化 sink 单独传递 LogMessage                         |
| Dashboard 需要查看最近日志       | 增加内存 RecentLogStore，保存格式化内容和结构化字段       |
| MySQL 连接失败不能影响普通日志   | 连接或写入失败时输出错误信息，但不影响其他 sink 继续工作  |

### 三、下周工作计划

- 实现 Web Dashboard 后端接口
- 实现 Dashboard 页面
- 增加手动生成日志和动态修改日志级别能力


## 第 14 周

### 一、工作内容

1. **Web Dashboard 后端实现**
   - 完成 `dashboard.rs`
   - 使用 axum 提供 HTTP 服务
   - 实现 `/metrics` 指标接口
   - 实现 `/logs/recent` 实时日志查询接口
   - 实现 `/logs/mysql` MySQL 历史日志查询接口
   - 实现 `/logs/generate` 手动生成日志接口
   - 实现 `/loggers/level` 动态修改 logger 级别接口

2. **Dashboard 前端页面实现**
   - 内置 HTML/CSS/JavaScript 页面
   - 展示总日志数、QPS、WARN、ERROR、错误率等指标
   - 展示各级别日志数量分布
   - 支持实时日志和 MySQL 历史日志切换
   - 支持级别筛选、关键字搜索、条数限制
   - 支持暂停刷新和继续刷新
   - 支持导出 JSON 和 TXT

3. **运行状态展示**
   - 增加 `runtime.rs`
   - 统计异步待处理字节数、异步批次数和丢弃日志数
   - Dashboard 展示异步处理批次数

### 二、遇到的问题与解决方案

| 问题                         | 解决方案                                            |
| ---------------------------- | --------------------------------------------------- |
| Dashboard 不希望依赖前端工程 | 直接在 Rust 中内置 HTML 页面，减少 npm 构建依赖     |
| 查询接口需要同时支持筛选搜索 | 通过 query 参数传递 limit、level、q，并在后端过滤   |
| 页面需要防止日志内容破坏结构 | 前端渲染时对日志内容进行 HTML 转义                  |

### 三、下周工作计划

- 增加配置文件读取
- 完善 Dashboard 与配置的集成
- 编写集成测试和最终文档


## 第 15 周

### 一、工作内容

1. **配置文件支持**
   - 完成 `config.rs`
   - 支持读取 `bitlog.toml`
   - 配置项包括 logger、dashboard、mysql、rolling、alert
   - 没有配置文件时使用默认值
   - Dashboard 启动时读取配置中的 host、port、MySQL URL 和表名

2. **Dashboard 配置集成**
   - 修改 Dashboard 启动逻辑
   - 默认从 `bitlog.toml` 读取监听地址和端口
   - MySQL 历史查询接口读取配置中的数据库 URL 和表名
   - 保留 `start_dashboard().await` 的原有调用方式，避免影响示例代码

3. **测试体系初步完善**
   - 编写 `tests/acceptance.rs`
   - 覆盖同步日志、异步多线程日志、结构化 sink、级别过滤、自定义格式、命名日志器、文件输出、滚动压缩、最近日志、统计、JSON formatter、配置默认值和动态修改 logger 级别
   - 运行 `cargo test` 验证全部测试通过

### 二、遇到的问题与解决方案

| 问题                                  | 解决方案                                               |
| ------------------------------------- | ------------------------------------------------------ |
| 配置文件缺失时不能影响项目运行        | 提供 BitLogConfig::load_or_default，失败时回退默认配置 |
| Dashboard 原先存在硬编码端口和数据库  | 接入 BitLogConfig，启动时读取 host、port、url、table   |
| MySQL 环境不是所有测试机器都具备      | 普通自动化测试不强依赖 MySQL，MySQL 写入用 demo 验证   |

### 三、下周工作计划

- 完善示例程序和 README
- 整理结题报告
- 进行最终功能检查和项目总结


## 第 16 周

### 一、工作内容

1. **示例程序完善**
   - 增加 `dashboard_demo.rs`，演示 Dashboard、实时日志和 MySQL 写入
   - 增加 `mysql_demo.rs`，单独验证 MySQL sink 写入流程
   - 检查 `basic.rs`、`async_demo.rs`、`file_sink.rs` 是否能覆盖主要使用场景

2. **README 和项目文档完善**
   - 完善 README
   - 补充 Dashboard、MySQL、配置文件、滚动策略和测试流程说明
   - 整理每周工作记录
   - 整理结题报告和项目说明

3. **最终功能检查**
   - 检查公开 API 是否可用
   - 检查 examples 是否能体现主要功能
   - 检查 Dashboard 接口和页面功能
   - 检查普通测试不强依赖 MySQL 服务
   - 统一运行 `cargo fmt` 和 `cargo test`

4. **项目收尾**
   - 对照 README 检查功能实现情况
   - 整理项目亮点和不足
   - 总结后续可优化方向

### 二、遇到的问题与解决方案

| 问题                       | 解决方案                                            |
| -------------------------- | --------------------------------------------------- |
| 功能较多，文档容易遗漏     | 按 README 功能列表逐项检查并补充说明                |
| 示例需要覆盖不同运行环境   | 将 MySQL 相关验证放到独立 demo，普通测试不强依赖它  |
| 最终验收需要证明功能可运行 | 使用 cargo fmt 和 cargo test 进行统一检查           |

### 三、项目总结

- 项目已经完成同步日志、异步日志、多 sink 输出、文件滚动、压缩清理、MySQL 持久化、Web Dashboard、配置文件、统计、panic hook、TUI 和自动化测试等功能。
- 项目从基础日志库扩展为具备可视化、查询、导出和动态控制能力的完整日志系统。
- 后续如果继续完善，可以重点优化异步队列实现、配置项接入范围、Dashboard 页面交互和日志宏的过滤前格式化开销。
